import { PublishDebtRejection } from './publish-debt-rejection.model';

describe('PublishDebtRejection', () => {
  it('should create an instance', () => {
    expect(new PublishDebtRejection()).toBeTruthy();
  });
});
