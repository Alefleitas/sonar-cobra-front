import { QuotationHistory } from './quotation-history.model';

describe('QuotationHistory', () => {
  it('should create an instance', () => {
    expect(new QuotationHistory()).toBeTruthy();
  });
});
