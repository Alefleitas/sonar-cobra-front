import { PublishDebinRequest } from './publish-debin-request';

describe('PublishDebinRequest', () => {
  it('should create an instance', () => {
    expect(new PublishDebinRequest()).toBeTruthy();
  });
});
