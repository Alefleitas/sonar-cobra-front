import { RepeatedClientEmail } from './auditory-profile-currency';

describe('RepeatedClientEmail', () => {
  it('should create an instance', () => {
    expect(new RepeatedClientEmail()).toBeTruthy();
  });
});
