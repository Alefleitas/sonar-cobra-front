import { PublishedDebtFile } from './published-debt-file.model';

describe('PublishedDebtFile', () => {
  it('should create an instance', () => {
    expect(new PublishedDebtFile()).toBeTruthy();
  });
});
